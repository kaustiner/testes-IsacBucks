/* ============================================================
   ISACBUCKS — INICIALIZAÇÃO
   Este arquivo só inicializa o sistema depois que os módulos
   anteriores já foram carregados.
   ============================================================ */

$('#retry-btn').addEventListener('click', boot);
boot();
